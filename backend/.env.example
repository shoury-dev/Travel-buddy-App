# Environment Configuration for Travel Buddy App

# Database Configuration
# Set to 'local' for JSON file database or 'mongo' for MongoDB
DATABASE_TYPE=local

# MongoDB Configuration (only needed if using MongoDB)
MONGODB_URI=mongodb://localhost:27017/travelbuddy
# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/travelbuddy

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend Configuration
FRONTEND_URL=http://localhost:5173
